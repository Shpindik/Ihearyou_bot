# IhearYou Bot Admin Frontend

Административная панель для управления ботом IhearYou, построенная на React с использованием современного стека технологий.

## 🚀 Технологический стек

- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS** - стилизация
- **React Query** - управление состоянием сервера
- **React Router** - маршрутизация
- **Axios** - HTTP клиент
- **Feature-Sliced Design** - архитектура проекта

## 📁 Структура проекта

```
src/
├── app/                    # Инициализация приложения
├── pages/                  # Страницы приложения
│   ├── dashboard/         # Главная панель
│   ├── login/             # Авторизация
│   └── error/             # Страница ошибок
├── entities/              # Бизнес-сущности
│   └── user/              # Пользователь
│       └── token/         # Токены авторизации
├── processes/             # Бизнес-процессы
│   └── router/            # Маршрутизация
├── shared/                # Переиспользуемые модули
│   ├── api/               # API клиент
│   ├── guards/            # Защищенные маршруты
│   ├── ui/                # UI компоненты
│   └── utils/             # Утилиты
└── main.tsx               # Точка входа
```

## 🛠 Установка и запуск

### Предварительные требования

- Node.js 18+
- Yarn или npm

### Установка зависимостей

```bash
yarn install
```

### Запуск в режиме разработки

```bash
yarn dev
```

Приложение будет доступно по адресу: http://localhost:5174

### Сборка для продакшена

```bash
yarn build
```

### Предварительный просмотр сборки

```bash
yarn preview
```

## 📜 Доступные скрипты

- `yarn dev` - запуск dev-сервера
- `yarn build` - сборка для продакшена
- `yarn build:watch` - сборка в режиме наблюдения
- `yarn build:dev` - сборка в dev-режиме
- `yarn lint` - проверка кода линтером
- `yarn prettier` - проверка форматирования
- `yarn prettier:fix` - автоисправление форматирования
- `yarn preview` - предварительный просмотр сборки

## 🎨 Стилизация

Проект использует **Tailwind CSS** с кастомной конфигурацией:

- Кастомные цвета и шрифты
- Анимации и переходы
- Адаптивная верстка
- Кастомные компоненты Material-UI

### Шрифты

Используется шрифт **Nekst** с различными начертаниями:

- Nekst-Thin
- Nekst-Light
- Nekst-Regular
- Nekst-Medium
- Nekst-SemiBold
- Nekst-Bold
- Nekst-Black

## 🔧 Конфигурация

### Переменные окружения

Создайте файлы в папке `environment/`:

- `.env.development` - для разработки
- `.env.production` - для продакшена
- `.env.staging` - для тестирования

Пример переменных:

```env
VITE_API_URL=http://localhost:8000
VITE_APP_NAME=IhearYou Admin
```

### TypeScript

Проект использует строгую типизацию TypeScript с:

- Строгими правилами ESLint
- Feature-Sliced Design архитектурой
- Автоматическими путями импорта

## 🏗 Архитектура

Проект следует принципам **Feature-Sliced Design**:

- **app** - инициализация приложения
- **pages** - страницы приложения
- **entities** - бизнес-сущности
- **processes** - бизнес-процессы
- **shared** - переиспользуемые модули

### Правила импортов

- Слои могут импортировать только из нижележащих слоев
- Запрещены циклические зависимости
- Публичный API через index.ts файлы

## 🔐 Авторизация

Система авторизации включает:

- JWT токены (access/refresh)
- Защищенные маршруты
- Автоматическое обновление токенов
- Перехватчики HTTP запросов

## 📱 Адаптивность

Приложение адаптировано для:

- Десктоп (1920px+)
- Планшеты (768px+)
- Мобильные устройства (320px+)

## 🧪 Качество кода

### Линтинг

- ESLint с правилами для React и TypeScript
- Feature-Sliced Design правила
- Unicorn правила для лучших практик

### Форматирование

- Prettier для единообразного стиля
- Husky для pre-commit хуков
- lint-staged для проверки измененных файлов

## 🚀 Деплой

### Docker

```bash
# Сборка образа
docker build -t ihearyou-admin-frontend .

# Запуск контейнера
docker run -p 3000:3000 ihearyou-admin-frontend
```

### Nginx

Пример конфигурации для Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
```

## 🤝 Разработка

### Добавление новых страниц

1. Создайте папку в `src/pages/`
2. Добавьте компонент страницы
3. Обновите роутер в `src/processes/router/`
4. Добавьте типы и интерфейсы

### Добавление новых сущностей

1. Создайте папку в `src/entities/`
2. Добавьте модели, API, компоненты
3. Экспортируйте через `index.ts`
4. Используйте в процессах и страницах

### Стилизация компонентов

- Используйте Tailwind CSS классы
- Создавайте кастомные компоненты в `shared/ui/`
- Следуйте дизайн-системе проекта

## 📞 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера
2. Убедитесь в корректности переменных окружения
3. Проверьте подключение к API
4. Обратитесь к команде разработки

---

**Версия:** 0.1.0
